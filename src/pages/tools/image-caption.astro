---
import ToolLayout from '../../components/ToolLayout.astro';
---

<ToolLayout
  title="Image Caption"
  description="Free AI image caption generator. Generate descriptive captions for images automatically. Great for accessibility, alt text, and content creation."
  icon="🏷️"
  category="Image"
  hfSpaceUrl="https://huggingface.co/spaces/LocoreMind/image-caption"
  keywords="image caption generator, AI image description, automatic alt text, image to text, accessibility tool, photo caption creator"
>
  <div class="space-y-6">
    <div>
      <label class="block text-sm font-medium text-surface-300 mb-2">Upload Image</label>
      <div id="drop-zone" class="border-2 border-dashed border-surface-700 rounded-lg p-10 text-center hover:border-surface-500 transition-colors cursor-pointer">
        <div class="text-4xl mb-3">🏷️</div>
        <p class="text-surface-400 text-sm">Drag & drop an image here, or click to browse</p>
        <p class="text-surface-500 text-xs mt-2">Supports PNG, JPG, WebP — Max 10MB</p>
        <input type="file" id="file-input" class="hidden" accept="image/*" />
      </div>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      <div>
        <label class="block text-sm font-medium text-surface-300 mb-2">Caption Style</label>
        <select class="w-full bg-surface-800 border border-surface-700 rounded-lg px-3 py-2 text-sm text-surface-200 focus:border-primary-500 focus:outline-none">
          <option>Descriptive</option>
          <option>Alt Text (Accessibility)</option>
          <option>Social Media</option>
          <option>Detailed Analysis</option>
        </select>
      </div>
      <div>
        <label class="block text-sm font-medium text-surface-300 mb-2">Model</label>
        <select class="w-full bg-surface-800 border border-surface-700 rounded-lg px-3 py-2 text-sm text-surface-200 focus:border-primary-500 focus:outline-none">
          <option>Qwen2-VL (Recommended)</option>
          <option>LLaVA-1.6</option>
          <option>BLIP-2</option>
        </select>
      </div>
    </div>

    <button id="run-btn" class="w-full py-3 bg-primary-600 hover:bg-primary-500 text-white font-medium rounded-lg transition-colors disabled:opacity-50 disabled:cursor-not-allowed" disabled>
      Generate Caption
    </button>

    <div id="result" class="hidden">
      <div class="flex items-center justify-between mb-2">
        <label class="block text-sm font-medium text-surface-300">Generated Caption</label>
        <button id="copy-btn" class="text-xs text-primary-400 hover:text-primary-300 transition-colors">Copy to clipboard</button>
      </div>
      <div class="bg-surface-800 border border-surface-700 rounded-lg p-4">
        <p id="result-text" class="text-sm text-surface-200 leading-relaxed">A professional workspace setup featuring a silver Mac Studio computer on a minimalist wooden desk, positioned next to a 27-inch display showing code in a dark-themed editor. The desk is neatly organized with a mechanical keyboard, wireless mouse, and a small potted plant providing a touch of green. Natural light streams in from a window on the left, creating soft shadows across the desk surface.</p>
      </div>
    </div>
  </div>
</ToolLayout>

<script>
  const dropZone = document.getElementById('drop-zone')!;
  const fileInput = document.getElementById('file-input') as HTMLInputElement;
  const runBtn = document.getElementById('run-btn') as HTMLButtonElement;
  const result = document.getElementById('result')!;
  const copyBtn = document.getElementById('copy-btn')!;
  const resultText = document.getElementById('result-text')!;

  dropZone.addEventListener('click', () => fileInput.click());
  dropZone.addEventListener('dragover', (e) => { e.preventDefault(); dropZone.classList.add('border-primary-500'); });
  dropZone.addEventListener('dragleave', () => dropZone.classList.remove('border-primary-500'));
  dropZone.addEventListener('drop', (e) => {
    e.preventDefault();
    dropZone.classList.remove('border-primary-500');
    if (e.dataTransfer?.files.length) handleFile(e.dataTransfer.files[0]);
  });
  fileInput.addEventListener('change', () => { if (fileInput.files?.length) handleFile(fileInput.files[0]); });

  function handleFile(file: File) {
    dropZone.innerHTML = `<div class="text-2xl mb-2">🖼️</div><p class="text-surface-300 text-sm font-medium">${file.name}</p><p class="text-surface-500 text-xs mt-1">${(file.size / 1024).toFixed(1)} KB</p>`;
    runBtn.disabled = false;
  }

  runBtn.addEventListener('click', () => {
    runBtn.textContent = 'Analyzing...';
    runBtn.disabled = true;
    setTimeout(() => {
      result.classList.remove('hidden');
      runBtn.textContent = 'Generate Caption';
      runBtn.disabled = false;
    }, 1500);
  });

  copyBtn.addEventListener('click', () => {
    navigator.clipboard.writeText(resultText.textContent || '');
    copyBtn.textContent = 'Copied!';
    setTimeout(() => copyBtn.textContent = 'Copy to clipboard', 2000);
  });
</script>
